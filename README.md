# Weather app

## Quick start

1.  Clone the repository `git clone https://github.com/HartwigH/weather-app.git
2.  Run initial install `npm install`
3.  Create .env `cp .env.example .env`
4.  Run dev `npm start`
